/*
  Warnings:

  - The primary key for the `_MyOrderToReport` table will be changed. If it partially fails, the table could be left without primary key constraint.

*/
-- AlterTable
ALTER TABLE `_MyOrderToReport` DROP PRIMARY KEY;
